services:
  - type: web
    name: halloween-discord-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: PORT
        value: 5000
      - key: SESSION_SECRET
        generateValue: true
